services:
  news-proxy:
    container_name: news-proxy
    build: .
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    environment:
      - NEWS_API_KEY=${NEWS_API_KEY}
      - PORT=${PORT:-8080}
    restart: unless-stopped
